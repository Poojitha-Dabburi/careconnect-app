specVersion: "v1beta"
serviceId: "careconnect"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "careconnect-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
